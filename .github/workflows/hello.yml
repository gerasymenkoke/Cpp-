name: C/C++ CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  
 hello:
   
       
    runs-on: ubuntu-latest
   

    steps:
       - uses: actions/checkout@v2
       # - uses: actions/checkout@v2
       
       - run:   ls -d */


      # - run:  ls -l
      # cd /
      
            # chmod +x hello

   # - name: make
   #   run: make
   # - name: make check
    #  run: make check
    # - name: make distcheck
     # run: make distcheck
